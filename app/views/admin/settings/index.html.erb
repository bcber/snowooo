<table class="table">
  <thead>
    <tr>
      <th>key</th>
      <th>value</th>
      <th>Operation</th>
    </tr>
  </thead>

  <tbody>
    <% @settings.each do |setting| %>
      <tr>
        <td><%= setting.key %></td>
        <td><%= setting.value %></td>
        <td><%= link_to "change", edit_admin_setting_path(setting)%></td>
      </tr>
    <% end %>
  </tbody>
</table>