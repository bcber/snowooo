<%= form_for [:admin, @video_node] do |f| %>
    <% if @video_node.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@video_node.errors.count, "error") %> prohibited this video_node from being saved:</h2>

          <ul>
            <% @video_node.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="form-group">
      <%= f.label :title %>
      <%= f.text_field :title ,class:"form-control"%>
    </div>

    <div class="form-group">
      <%= f.label :description %>
      <%= f.text_field :description, class:"form-control" %>
    </div>

    <div class="actions">
      <%= f.submit '确认', class:"btn btn-default btn-xs"%>
    </div>
<% end %>
