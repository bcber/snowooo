<%= form_for(@snowboot) do |f| %>
  <% if @snowboot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@snowboot.errors.count, "error") %> prohibited this snowboot from being saved:</h2>

      <ul>
      <% @snowboot.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
